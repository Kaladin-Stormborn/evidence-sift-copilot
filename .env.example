# Optional. If the local Codex CLI is configured with a model provider, the
# agentic runner will use it. Otherwise use --offline for deterministic fallback.
OPENAI_API_KEY=
EVIDENCE_SIFT_MODEL=gpt-5.5
